<%= form_for(@sales_channel) do |f| %>
  <% if @sales_channel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sales_channel.errors.count, "error") %> prohibited this sales_channel from being saved:</h2>

      <ul>
      <% @sales_channel.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :comment %><br />
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
